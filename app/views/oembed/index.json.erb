
{
  "type": "<%= @post.type.name %>",
  "version": 1.0,
  "height": 300,
  "width": 300,
  "url": "<%= post_url(@post) %>",
  "thumbnail_url": "<%= @post.image %>",
  "html": "<%=h @post.embed_code.gsub(/["]/, "'").html_safe %>"
}
